<h1>Hallo Welt</h1>

<ul>
  <% @articles.each do |article| %>
    <li>
      <h2>
        <%= link_to article.title, article_path(article) %>
      </h2>
      <div>
        <%= link_to 'Artikel editieren', edit_article_path(article) %>
        <%= button_to 'Artikel löschen', article_path(article), method: :delete %>
      </div>
    </li>
  <% end %>
</ul>
